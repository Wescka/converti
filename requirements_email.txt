# No hay dependencias adicionales para EML/MSG.
# El visor usa el parser integrado en app.py y las dependencias ya existentes de Converti.
# NO instalar extract-msg, cryptography, maturin ni Rust para esta función.
